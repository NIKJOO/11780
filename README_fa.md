# 🛡️ SecureE2E 

> [!NOTE] 
> **این پروژه به همه کسانی که در جریان انقلاب ملی ایران جان خود را از دست دادند تقدیم می‌شود؛ کسانی که نامشان را برای همیشه به یاد خواهیم داشت، کسانی که نامشان هرگز بر زبان آورده نمی‌شود، و به ویژه شهید گمنام شماره ۱۱۷۸۰**.



# 📖 درباره
 یک برنامه چت کلاینت-سرور است که در آن سرور به عنوان یک واسط خنثی عمل می‌کند. این برنامه تضمین می‌کند که حتی اگر سرور مورد حمله قرار گیرد، محتوای پیام‌های شما خصوصی باقی بماند.

## این پروژه یک پروتکل رمزنگاری سفارشی پیاده‌سازی می‌کند که ویژگی‌های زیر را دارد:
- الگوریتم Double Ratchet: امنیت پیام‌های گذشته و آینده را تضمین می‌کند. هر پیام از یک کلید رمزنگاری منحصر به فرد استفاده می‌کند.
- دست دادن نامتقارن (Asymmetric Handshake): از **Ed25519** برای امضای هویت و **Curve25519** برای تبادل کلید استفاده می‌کند.
- جداسازی مسئولیت‌ها: رمز عبور سرور دسترسی به شبکه را محافظت می‌کند، در حالی که کلیدهای E2E محتوای پیام‌های شما را محافظت می‌کنند.

#
# ✨ ویژگی‌ها
- 🔒 رمزنگاری واقعی End-to-End: پیام‌ها در دستگاه شما رمزگذاری شده و تنها در دستگاه گیرنده رمزگشایی می‌شوند. سرور نمی‌تواند آن‌ها را بخواند.
- 🔄 امنیت کامل پیام‌های گذشته (Perfect Forward Secrecy): لو رفتن یک کلید بلندمدت پیام‌های گذشته را فاش نمی‌کند.
- 🆔 تأیید هویت: هر کاربر اثر انگشت دیجیتالی منحصر به فردی ایجاد می‌کند. این اثر انگشت را خارج از کانال امن بررسی کنید تا مطمئن شوید که تحت حمله Man-in-the-Middle (MitM) قرار نگرفته‌اید.
- 🛡️ انتقال امن: اتصال به سرور با استفاده از **AES-256-GCM** که از طریق Scrypt مشتق شده، ایمن می‌شود.
- 🚫 جلوگیری از ارسال پیام به خود: منطق برنامه از حلقه‌ها و خطاهای مسیریابی جلوگیری می‌کند.
- 📜 صف‌بندی پیام‌ها: اگر قبل از اتمام دست دادن رمزنگاری شروع به نوشتن کنید، پیام‌ها به‌طور امن صف‌بندی شده و پس از امن شدن کانال به صورت خودکار ارسال می‌شوند.
#
# 🔐 معماری امنیتی
مدل امنیتی در دو لایه مجزا عمل می‌کند:

1. لایه انتقال (Client ↔ Server) :
    + _هدف: جلوگیری از اتصال کاربران غیرمجاز به سرور چت._
    + _مکانیزم: Scrypt Key Derivation + AES-256-GCM._
    + _کلید: مشتق شده از رمز عبور سرور._
2. لایه کاربردی (Client ↔ Client) :
    + _هدف: تضمین اینکه تنها گیرنده مورد نظر بتواند محتوای پیام را بخواند._
    + _مکانیزم: Double Ratchet (الهام گرفته از X3DH)._
3. کلیدها :
    + _کلید هویت: Ed25519._
    + _کلید موقت: Curve25519._
    + _Ratchet: هر دو طرف یک کلید زنجیره‌ای نگهداری می‌کنند. ارسال پیام زنجیره ارسال را پیش می‌برد و یک کلید پیام منحصر به فرد تولید می‌کند._

#
# 📦 نصب
> بهینه‌سازی شده برای سیستم عامل ویندوز (هم کلاینت و هم سرور). برای محیط لینوکس نیاز به تغییراتی دارید.

پیش‌نیازها
  + نصب Go نسخه 1.21 یا بالاتر.
  + دسترسی به ترمینال.

1. کلون کردن مخزن
<pre>    git clone https://github.com/yourusername/SecureE2E-Go.git
    cd SecureE2E-Go </pre>
2. نصب وابستگی‌ها
<pre>
    go get golang.org/x/crypto/curve25519
    go get golang.org/x/crypto/ed25519
    go get golang.org/x/crypto/hkdf
    go get golang.org/x/crypto/scrypt
    go get golang.org/x/crypto/ssh/terminal
    go get golang.org/x/crypto/argon2
</pre>
3. ساخت برنامه
<pre>
    # ساخت سرور
    go build -o server server.go
    
    # ساخت کلاینت
    go build -o client client.go
</pre>
#
# 🚀 استفاده
دانلود ویدیو (قابل پیش‌نمایش در وب نیست) : ([لینک](https://github.com/user-attachments/assets/e6c4155e-8701-4228-87a4-0c989255ba3f)).

## دنبال کردن من

- **X (توییتر):** [https://x.com/N_Nikjoo](https://x.com/N_Nikjoo)  
- **LinkedIn:** [https://www.linkedin.com/in/nimanikjoo/](https://www.linkedin.com/in/nimanikjoo/)  
- **کانال تلگرام:** [https://t.me/VSEC_academy](https://t.me/VSEC_academy)

#
📄 مجوز
توزیع شده تحت مجوز MIT.
